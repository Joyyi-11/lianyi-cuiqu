---
name: lianyi-cuiqu
description: |
  从外部优秀经验（Skill、提示词、规则文档、文章、工作流、产品方法、真实反馈）中萃取精华，再把精华吸收内化进自己的能力框架，持续打磨、提升自己。以用户自己的问题为判断主体，对每项内容作出六种处理决定——absorb / reference / create / observe / reject / retire——映射进自己的框架。
  触发条件（命中任一即先加载本 skill，不要直接去编辑目标 skill）：用户说「拆解这个」「帮我萃取」「借鉴这个框架」「分析它为什么好用」「把这个方法/思路收进/补进/写进我的 skill」；或要求把某篇外部文章、某个方法、某段规则或某个 Skill 的能力「吸收 / 内化 / 融合 / 提炼」进自己任一 skill 或方法体系（典型说法：「把这里提到的 X 吸收进我的 writing skill」「把这个思路融入我的系统」）；或正在从优秀案例和真实反馈建立、补充、重构个人方法体系。
  路由铁律：当请求同时指向「某外部来源」和「某目标 skill / 方法体系」时，本 skill 接管吸收动作（步骤 6 决定如何写入），不要把它当成对该 skill 的直接编辑任务而跳到落盘；吸收前必须先走「来源还原 → 逐块核销 → 处理决定 → 目标映射」四步。
  不用于只需摘要、普通评价、逐行翻译或机械复制原文的任务。
---

# 萃取

## 核心定位

帮助用户从外部优秀经验中萃取精华，拆解其结构、能力和原则，吸收内化进自己的系统，持续打磨、提升自己。来源只提供质量参照、可能性和经验，不是标准答案。最终框架必须同时来自用户自己的目标、实践经验、来源中的有效结构，以及用户主动补充的新方法。

始终遵守：

> 保留来源结构来理解内容，以自己的问题作判断，再把有效能力安置到目标结构，并通过真实使用验证。

## 角色边界

AI 负责：

- 搜索和读取真实来源，重建标题层级与内容关系。
- 按内容块精读，区分可观察事实、设计决策与可能原因。
- 识别能力、原则、适用条件、重复、冲突、缺口和放置位置。
- 提出处理决定（`absorb` / `reference` / `create` / `observe` / `reject` / `retire`）和验证方案。
- 保留来源、推测和用户决策之间的边界。

用户负责：

- 定义什么是好、自己真正要解决什么问题。
- 判断哪些内容适合自己，哪些需要改造或放弃。
- 用自己的经验补充来源没有覆盖的部分。
- 决定冲突时保留什么，以及系统最终长成什么样。

不要替用户作最终取舍。用户没有明确要求修改目标文件时，只输出分析和方案；用户要求执行时，先列出目标位置、改动内容和影响，得到确认后再写入。

## 触发路由（防盲目吸收）

只要请求涉及「把外部来源吸收 / 内化 / 融合 / 提炼进用户自己的 skill 或方法体系」，无论用户用的是「吸收」「收进」「补进」「写进」「融入」哪个动词，都先加载本 skill，不要把它当成对目标 skill 的直接编辑而跳到落盘。吸收动作由本 skill 步骤 6 接管：先完成来源还原、逐块核销、处理决定与目标映射，再列文件级方案、确认后写入。

典型漏触发的说法：「把这里提到的 X 吸收进我的 writing skill」——表面指向目标 skill，实质是萃取任务。本 skill 才是吸收动作的 owner，目标 skill 只是被写入的对象，不是入口。

## 工作流

执行实际任务时读取 `references/analysis-template.md`。需要理解边界、反例或方法来源时，再读取 `references/case-studies.md`。需要设计或优化连漪自己的提示词时，读取 `references/prompt-design.md`。

### 执行顺序硬约束

顺序不得颠倒（跨来源归并必须在逐块核销之后）；深度按任务复杂度缩减，但来源还原、处理决定和目标映射三步不得省。步骤 6（融入目标框架）和步骤 8（真实使用验证）仅在执行任务时必做，纯分析任务可标暂缓。完整顺序如下：

```text
锚定自己的问题和目标缺口
  → 建立质量参照（来源是否值得研究、综合还是专项）
  -> 枚举来源文件、记录或版本
  → 还原来源结构
      文档 / 文章 / Skill：标题层级与内容块
      实践 / 对话 / 修改历史：任务阶段、用户纠正与前后版本
  → 逐个内容块或反馈单元核销
  → 还原内容块之间的流程和依赖
  → 与目标框架逐块对照
  → 按能力跨来源归并
  → 形成吸收方案
  → 判断整体状态和退役条件
```

先完成来源审计，再做能力归并。不能因为目标系统出现了近义句、README 声称已经整合、某个文件内容相同，或核心思想已经出现，就推断整个来源已经被吸收。

结构化来源的每个内容块都必须进入核销表。实践反馈则先把“发生了什么、用户怎样纠正、前后结果有什么变化”还原为反馈单元，再进入核销。仍有未核销内容块或反馈单元时，不得输出“完全吸收”“可以删除”或 `retire` 结论。

### 1. 锚定自己的问题

先明确：

- 用户想解决什么问题，成功标准是什么？
- 用户已有怎样的经验、判断和偏好？
- 当前有没有自己的框架？哪里已经稳定，哪里仍有缺口？
- 这次需要理解来源、形成方案，还是实际融入目标系统？

不要把“我觉得它好”直接当作吸收理由。继续追问它解决了用户哪个尚未解决的问题、补足了什么缺口，或让用户看见了什么原本没有看见的东西。无法落到真实问题或能力缺口时，先保留观察，不把收藏冲动升级为长期规则。

如果用户已有框架，先读取真实状态并画出当前结构。没有框架时，不把来源完整复制为地基；从用户要解决的问题反推需要的能力，列成问题清单作为初步骨架，再把来源作为质量参照和能力输入填入。

### 2. 建立质量参照

判断来源为什么值得研究：是否经过真实使用，是否代表较高质量，是否与用户问题相关，是否有足够证据支持。来源优秀不等于全部适用。

区分两类来源：

- **综合来源**：展示相对完整的成熟流程，帮助看见领域全貌。
- **专项来源**：只补一个能力缺口，例如标题、审阅、结构分析或反例验证。

不要让来源的完整度决定目标框架的形状。综合来源要警惕把整条流程照搬，专项来源只需核销它补的那个缺口。

### 3. 按内容块拆解

先枚举来源目录中的 `SKILL.md`、`references/`、脚本、模板、资产说明和元数据，说明哪些文件参与能力判断、哪些只是许可证或辅助文件。再还原全文目标、标题层级和内容块，进入块内逐段、逐行阅读：

```text
全文目标
  → 标题层级
    → 小标题内容块
      → 块内段落和语句
```

小标题定义内容的作用域。任何句子都必须结合所属标题解释。若分析脱离标题逐行罗列，停止当前结果，重新按内容块拆解，不把错误分析带入吸收阶段。

核销以标题节点为单位，自上而下按层级进行。先确认每个一级标题承担的语义任务，再下钻其下的二、三级标题；下级标题从属于上级，是后者能力的展开或子步骤，不抽离成孤立能力判断。核销表为每个块标注标题层级，还原父标题到子标题的从属关系。

不要把一个流程型能力拆成互不相干的句子。除逐块分析外，还要还原内容块之间的先后、依赖和共同完成的结果。例如“姿态 → 过程 → 写作手法 → 打磨 → 验收”是一条能力链；目标系统只出现其中一环，不代表整条能力已经吸收。

对每个内容块回答：

1. 它解决了什么问题？
2. 它在什么场景下使用？
3. 它为什么这样设计或表达？
4. 它依赖哪些前提、限制和上下文？
5. 哪些是可迁移结构，哪些只是作者的个人风格或局部实现？
6. 它与用户现有框架有什么关系？
7. 它进入用户框架后应当放在哪里？

使用“观察事实 → 识别决策 → 推测原因”的链条。没有作者说明时，把原因标记为推测，不把意图当事实。

逐块对照时区分四个层次：

- **文字覆盖**：目标中出现相同或近似表达。
- **原则覆盖**：底层判断已经存在，但流程、触发或边界可能缺失。
- **能力覆盖**：目标能够在相同场景完整完成来源承担的任务。
- **独立价值**：即使写作或局部场景已经覆盖，来源是否仍服务于不同任务或独立触发场景。

只有能力覆盖成立，才能把该内容块判为“已吸收”。文字覆盖和原则覆盖只能证明存在关联。

脚本、模板与可执行资产不按标题内容块核销，按其行为、输入输出和依赖判断能力：`absorb` 纳入其行为（直接采用或重写实现），`reference` 只保留路径，不逐行当文本块分析。

### 4. 提炼能力与原则

不要按来源名称组织结果，也不要只摘录原文。把内容转化为：

```text
原文内容
  → 解决的问题
  → 提供的能力
  → 生效条件
  → 可迁移原则
  → 用户框架中的位置
```

按“它教会了什么原则”归类，而不是按表面风格、措辞或来源归类。多个来源提供相同能力时，合并语义，保留各自有效边界。

跨来源归并只能发生在逐来源、逐内容块核销之后。最终方案可以按目标能力组织，但必须保留回指关系，使每项判断都能追溯到来源文件、标题和内容块。

单来源任务时，跨来源归并退化为逐块对照与目标映射，跳过 `references/analysis-template.md` 第五节。

分析顺序与最终呈现顺序分开。分析时保留来源结构，避免误读；形成方案、文章或方法说明时，优先沿目标问题、能力缺口、关键判断或解决路径组织。来源名称、事件时间线和个人经历只在它们能提供证据或认知转折时进入前台，不默认成为最终结构。

### 5. 作出处理决定

为每项内容选择动作。核销即给出以下一种动作；`未核销为 0` 指每块都有明确动作，不是每块都要 `absorb`：

- `absorb`：纳入目标框架（直接采用、改造、合并或换位；具体方式记在改造方式列）。
- `reference`：不复制内容，只保留真实来源或路径。
- `create`：来源暴露了缺口，由用户经验和判断创造新解法。
- `observe`：证据不足，暂不进入正式框架，记入观察列表（目标框架的待办或观察文件），附待验证假设与触发条件，新证据出现时复核。
- `reject`：与用户目标、场景或价值冲突。
- `retire`：曾经适用，但已过时、重复或被更好方案替代；在目标系统中停止引用并记录理由，删除来源文件是独立红线动作，需单独授权。

使用 `retire` 前，逐项核对退役门槛六项：内容块已处理、触发与边界已承接、完整流程未拆散、无独立资产残留、已设计验证任务、用户已单独授权删除或迁移（清单见 `references/analysis-template.md` 第十章）。任一不成立，只能判为“部分吸收”“保留独立能力”或“继续观察”，不能建议直接删除。

裁决优先级：

```text
用户的目标与最终判断
  > 真实使用反馈
  > 用户长期积累的经验
  > 外部来源的有效结构
  > 来源的具体文字
  > AI 的建议
```

### 6. 融入目标框架

按用户自己的结构安置内容，不照搬来源目录。融合时：

- 统一概念、语言、边界和工作流，消除拼贴感。
- 优先修改已有规则，不机械追加近义条目。
- 明确唯一真实源，写入前先核验目标路径本身是不是真实源（skills 目录常为 junction／symlink，真实源在工作区）；摘要、细则和适配器不要保存重复副本。
- 根据使用频率、稳定程度、责任归属、内容规模和真实来源决定位置。
- 允许新内容填入、合并、拆分或推动目标框架重构。
- 保留来源和改造说明，但不让来源身份盖过用户自己的系统。

### 7. 区分内容问题与组织问题

融入后或真实使用中出现问题时，先诊断：

- **内容缺失**：缺少必要能力或规则。
- **内容错误**：规则不适用或判断错误。
- **内容冲突**：多个规则无法同时成立。
- **组织不合理**：内容没错，但位置、长度、加载方式或职责影响遵循率。
- **触发失败**：内容存在，却没有在正确场景加载或执行。

只有内容问题才改方法本身。组织问题优先调整结构、加载和真实源，不假装是内容失效。

### 8. 在真实使用中验证

吸收完成不等于真正内化。把原则用于真实任务或新的相似问题，检查：

- 是否解决了原本的问题？
- 是否符合用户的表达、习惯、边界和场景？
- 是否与已有系统发生冲突或重复？
- 是否在正确时机被触发和遵循？
- 用户能否解释为什么保留它，以及什么情况下不用？

准备把单个案例升级为通用规则时，提高证据门槛：使用多个独立案例，区分不变量、合理变化和偶然相似，并用反例或未参与归纳的新案例检验。个人偏好或局部改造不强制执行这一门槛。

### 9. 持续演化

不要把一次反馈立刻固化为长期规则。反复出现、稳定影响结果或揭示结构性问题时，再增加、删除、移动、改写或重构。

从实践反馈更新系统时，使用以下链条：

```text
可观察问题
  → 用户怎样纠正
  → 问题为什么发生
  → 一次性措辞还是稳定能力缺口
  → 应进入哪个职责文件
  → 用什么任务验证
```

同一根因引发的多条细碎修改应合并为一项能力更新。例如多处病句可能共同指向语序和动宾搭配检查，不要把每个错句都写成独立规则。已有规则本来足够、只是没有执行时，优先补执行门槛、检查证据或测试，不继续堆近义原则。

当前系统只保存仍然有效的状态；来源记录保存知识谱系；Git 保存演化历史。曾经吸收过，不是继续保留的理由。

## 输出要求

按任务复杂度缩减。最小任务至少交付结论、核销摘要和文件级方案；完整分析至少交付：

1. 用户目标和当前框架。
2. 来源文件清单、纳入范围和标题结构图。
3. 内容块核销表及未核销数量。
4. 按内容块完成的拆解与流程关系。
5. 文字覆盖、原则覆盖、能力覆盖和独立价值判断。
6. 能力、原则与适用边界。
7. 每项内容的处理决定及理由。
8. 文件级吸收方案：目标位置、具体改法、与已有内容如何处理。
9. 整体状态、退役条件和冲突说明。
10. 需要用户最终判断的事项。
11. 验证方式和后续观察点。

避免输出没有作用域的逐行点评、只有优缺点的普通评价、按来源分章的拼贴方案，以及未经用户授权直接修改长期系统。

## 输出前自检

在向用户输出前逐项检查，不展示检查过程；任一项不通过，先修正结果：

- [ ] 已读取用户要求范围内的全部来源文件，没有只看 `SKILL.md`。
- [ ] 已列出来源标题结构，每个有效内容块都进入核销表，未核销数为 0。
- [ ] 每个判断都保留了标题作用域，没有把孤立句子当成完整能力。
- [ ] 已还原内容块之间的流程、依赖和共同结果，没有把能力链压成零散原则。
- [ ] 没有把近义文字、相同文件或 README 的整合声明直接当作“完整吸收”证据。
- [ ] 已区分文字覆盖、原则覆盖、能力覆盖和独立价值。
- [ ] 对 `absorb`、`reject`、`retire` 都给出了来源依据和适用边界。
- [ ] 吸收方案已落到目标文件或结构位置，并说明新增、改写、合并、引用或淘汰方式。
- [ ] 没有把所有内容机械塞进一个文件，也没有按来源目录照搬目标结构。
- [ ] `retire` 结论已逐项核对退役门槛六项（内容/触发边界/流程/独立资产/验证/授权）。
- [ ] 来源事实、AI 推测、用户经验和用户决定清晰分开。
- [ ] 没有替用户作最终取舍，也没有把分析授权扩大为修改、迁移或删除授权。
- [ ] 新框架以用户自己的问题和结构为中心，不是来源的变体；重复和冲突已处理，而非靠追加规则掩盖。
