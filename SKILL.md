---
name: lianyi-absorb
description: |
  拆解并吸收优秀 Skill、提示词、规则文档、文章、工作流、产品方法或其他作品。以用户自己的问题、经验和判断为主体，先按来源标题结构理解内容，再提炼其解决的问题、适用场景、设计原因和可迁移结构，决定直接采用、改造、合并、自主创造、迁移、引用、观察、拒绝或淘汰，并映射进用户自己的框架。适用于用户说“拆解这个”“看看哪些能吸收”“借鉴这个框架”“把别人的好方法融入我的系统”“分析它为什么好用”，或正在从优秀案例建立、补充、重构个人方法体系时。不用于只需摘要、普通评价、逐行翻译或机械复制原文的任务。
---

# 拆解吸收

## 核心定位

帮助用户把外部优秀经验转化为自己的能力和系统。优秀作品只提供质量参照、可能性和经验，不是标准答案。最终框架必须同时来自用户自己的目标、实践经验、外部来源的有效结构，以及用户主动补充的新方法。

始终遵守：

> 按来源结构理解，按自己的问题判断，按目标结构吸收，通过真实使用验证。

## 角色边界

AI 负责：

- 搜索和读取真实来源，重建标题层级与内容关系。
- 按语义块精读，区分可观察事实、设计决策与可能原因。
- 识别能力、原则、适用条件、重复、冲突、缺口和放置位置。
- 提出吸收、改造、合并、创造、迁移和验证方案。
- 保留来源、推测和用户决策之间的边界。

用户负责：

- 定义什么是好、自己真正要解决什么问题。
- 判断哪些内容适合自己，哪些需要改造或放弃。
- 用自己的经验补充来源没有覆盖的部分。
- 决定冲突时保留什么，以及系统最终长成什么样。

不要替用户作最终取舍。用户没有明确要求修改目标文件时，只输出分析和方案；用户要求执行时，先列出目标位置、改动内容和影响，得到确认后再写入。

## 工作流

执行实际任务时读取 `references/analysis-template.md`。需要理解边界、反例或方法来源时，再读取 `references/case-studies.md`。

### 1. 锚定自己的问题

先明确：

- 用户想解决什么问题，成功标准是什么？
- 用户已有怎样的经验、判断和偏好？
- 当前有没有自己的框架？哪里已经稳定，哪里仍有缺口？
- 这次需要理解来源、形成方案，还是实际融入目标系统？

如果用户已有框架，先读取真实状态并画出当前结构。没有框架时，不把来源完整复制为地基；结合用户目标和已有经验，先形成一个属于用户的初步骨架，再把来源作为质量参照和能力输入。

### 2. 建立质量参照

判断来源为什么值得研究：是否经过真实使用，是否代表较高质量，是否与用户问题相关，是否有足够证据支持。来源优秀不等于全部适用。

区分两类来源：

- **综合来源**：展示相对完整的成熟流程，帮助看见领域全貌。
- **专项来源**：只补一个能力缺口，例如标题、审阅、结构分析或反例验证。

不要让来源的完整度决定目标框架的形状。

### 3. 按语义块拆解

先还原全文目标、标题层级和内容块，再进入块内逐段、逐行阅读：

```text
全文目标
  → 标题层级
    → 小标题内容块
      → 块内段落和语句
```

小标题定义内容的作用域。任何句子都必须结合所属标题解释。若分析脱离标题逐行罗列，停止当前结果，重新按内容块拆解，不把错误分析带入吸收阶段。

对每个内容块回答：

1. 它解决了什么问题？
2. 它在什么场景下使用？
3. 它为什么这样设计或表达？
4. 它依赖哪些前提、限制和上下文？
5. 哪些是可迁移结构，哪些只是作者的个人风格或局部实现？
6. 它与用户现有框架有什么关系？
7. 它进入用户框架后应当放在哪里？

使用“观察事实 → 识别决策 → 推测原因”的链条。没有作者说明时，把原因标记为推测，不把意图当事实。

### 4. 提炼能力与原则

不要按来源名称组织结果，也不要只摘录原文。把内容转化为：

```text
原文内容
  → 解决的问题
  → 提供的能力
  → 生效条件
  → 可迁移原则
  → 用户框架中的位置
```

按“它教会了什么原则”归类，而不是按表面风格、措辞或来源归类。多个来源提供相同能力时，合并语义，保留各自有效边界。

### 5. 作出处理决定

为每项内容选择动作：

- `adopt`：结构、场景和边界都适合，直接采用。
- `adapt`：原理适合，按用户场景重新表达或实现。
- `merge`：与已有内容或其他来源语义合并。
- `create`：来源暴露了缺口，由用户经验和判断创造新解法。
- `relocate`：内容有效，但应移动到更合适的位置。
- `reference`：不复制内容，只保留真实来源或路径。
- `observe`：证据不足，暂不进入正式框架。
- `reject`：与用户目标、场景或价值冲突。
- `retire`：曾经适用，但已过时、重复或被更好方案替代。

裁决优先级：

```text
用户的目标与最终判断
  > 真实使用反馈
  > 用户长期积累的经验
  > 外部来源的有效结构
  > 来源的具体文字
  > AI 的建议
```

### 6. 融入目标框架

按用户自己的结构安置内容，不照搬来源目录。融合时：

- 统一概念、语言、边界和工作流，消除拼贴感。
- 优先修改已有规则，不机械追加近义条目。
- 明确唯一真实源；摘要、细则和适配器不要保存重复副本。
- 根据使用频率、稳定程度、责任归属、内容规模和真实来源决定位置。
- 允许新内容填入、合并、拆分或推动目标框架重构。
- 保留来源和改造说明，但不让来源身份盖过用户自己的系统。

### 7. 区分内容问题与组织问题

实际使用出现问题时，先诊断：

- **内容缺失**：缺少必要能力或规则。
- **内容错误**：规则不适用或判断错误。
- **内容冲突**：多个规则无法同时成立。
- **组织不合理**：内容没错，但位置、长度、加载方式或职责影响遵循率。
- **触发失败**：内容存在，却没有在正确场景加载或执行。

只有内容问题才改方法本身。组织问题优先调整结构、加载和真实源，不假装是内容失效。

### 8. 在真实使用中验证

吸收完成不等于真正内化。把原则用于真实任务或新的相似问题，检查：

- 是否解决了原本的问题？
- 是否符合用户的表达、习惯、边界和场景？
- 是否与已有系统发生冲突或重复？
- 是否在正确时机被触发和遵循？
- 用户能否解释为什么保留它，以及什么情况下不用？

准备把单个案例升级为通用规则时，提高证据门槛：使用多个独立案例，区分不变量、合理变化和偶然相似，并用反例或未参与归纳的新案例检验。个人偏好或局部改造不强制执行这一门槛。

### 9. 持续演化

不要把一次反馈立刻固化为长期规则。反复出现、稳定影响结果或揭示结构性问题时，再增加、删除、移动、改写或重构。

当前系统只保存仍然有效的状态；来源记录保存知识谱系；Git 保存演化历史。曾经吸收过，不是继续保留的理由。

## 输出要求

分析阶段至少交付：

1. 用户目标和当前框架。
2. 来源结构图。
3. 按内容块完成的拆解。
4. 能力、原则与适用边界。
5. 每项内容的处理决定及理由。
6. 目标结构映射和冲突说明。
7. 需要用户最终判断的事项。
8. 验证方式和后续观察点。

避免输出没有作用域的逐行点评、只有优缺点的普通评价、按来源分章的拼贴方案，以及未经用户授权直接修改长期系统。

## 完成标准

- 每个判断都能追溯到来源内容块、用户经验或真实使用证据。
- 来源的事实、AI 的推测和用户的决定清晰分开。
- 新框架以用户自己的问题和结构为中心，不是来源的变体。
- 重复和冲突得到处理，不靠继续追加规则掩盖。
- 方案包含可执行的目标位置和验证方法。
- 最终决定仍由用户作出。
